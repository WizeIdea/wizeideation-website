{
  "compilerOptions": {
    "target": "es2022",
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": false,                     // ⬅️  changed: we want to emit JS files
    "outDir": "dist",                    // ⬅️  new: where compiled JS goes
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "types": [
      "node"
    ],                                   // <-- keep this line (you already have it)
    "baseUrl": ".",                      // <-- keep – enables absolute imports
    "paths": {
      "@/*": [
        "./*"
      ]
    },
    "plugins": [
      {
        "name": "next"
      }
    ]
  },

  // 👇 we add the `scripts/**/*.ts` pattern so the helper gets compiled
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "scripts/**/*.ts"                    // <-- new: compile the copy‑script
  ],

  "exclude": [
    "node_modules"
  ]
}
